name: Deploy

on: 
  push

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:      
      - uses: actions/checkout@v4

      - name: Debug
        run: |
            pwd
            echo "ssh connection"
            sshpass -p teste*01 scp -o StrictHostKeyChecking=no /tmp/ teste@18.233.162.151:/home/teste


            

      # - name: copy file via ssh password
      #   uses: appleboy/scp-action@v0.1.7
      #   with:
      #     host: 3.85.128.169
      #     username: guilherme
      #     password: teste*01
      #     port: 22
      #     source: ./.github
      #     target: /c/Users/guilherme/Desktop/repo/
      #     tar_dereference: true 
      #     rm: true
      #     debug: true

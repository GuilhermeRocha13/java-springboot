name: Deploy
on: 
  push:
   branches:
    - deploy-windows
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:      
      - uses: actions/checkout@v4
      - name: SFTP Transfer
        run: |
             sshpass -p teste*02 sftp -o StrictHostKeyChecking=no -oBatchMode=no -b - ftp_user@44.203.253.118 << !
             cd Share/
             put -r /home/runner/work/gofree-ingressos-site-apis/gofree-ingressos-site-apis
             bye
             !
      - name: IIS Deploy
        run: 
            sshpass -p teste*01 ssh -o StrictHostKeyChecking=no guilherme@44.203.253.118 'sh /c/Users/ftp_user/Share/teste.bat'

name: deploy
on: 
  push:
    branches: pipeline-teste
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:      
    - uses: actions/checkout@v4
    - name: copy file via ssh password
      uses: appleboy/scp-action@v0.1.7
      with:
        host: 52.23.236.101
        username: guilherme
        password: teste*01
        port: 22
        source: ./*
        target: /c/Users/guilherme/Desktop/repo
        tar_dereference: true
        #rm: true
